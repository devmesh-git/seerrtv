# Signing configuration template for SeerrTV
# Copy this file to signing.properties and fill in your actual values
# This template file can be committed to git, but signing.properties should be in .gitignore

# Path to the keystore file (relative to project root)
storeFile=../localSigningKey.jks

# Keystore password
storePassword=your_keystore_password_here

# Key alias
keyAlias=your_key_alias_here

# Key password
keyPassword=your_key_password_here 